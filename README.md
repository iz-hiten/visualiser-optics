# visualiser-optics
web application where a user can design an optical setup visually and export it as a JSON file. Later, connect it to a backend for basic simulations and deploy the complete project online.
